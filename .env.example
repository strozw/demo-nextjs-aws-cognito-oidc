# vim: set ft=sh:

# AWS Cognito Configuration

# Cognito User Pool のURL（例: https://cognito-idp.your-region.amazonaws.com/your-user-pool-id）
COGNITO_USER_POOL_URL=https://cognito-idp.your-region.amazonaws.com/your-user-pool-id

# Cognito User Pool の認証ドメイン名（例: your-user-pool-id-sub-domain.auth.your-region.amazoncognito.com）
# ログアウトURL等の生成に利用 (your-user-pool-id-sub-domain には `_` が含まれていない点に注意)
COGNITO_AUTH_URL=https://your-user-pool-id-sub-domain.auth.your-region.amazoncognito.com

# Cognito Application Client ID
COGNITO_CLIENT_ID=your-client-id

# Cognito Application Client Secret
COGNITO_CLIENT_SECRET=your-client-secret

# コールバックURL（開発環境）
COGNITO_REDIRECT_URI=http://localhost:3000/api/auth/callback

# ログアウト後のリダイレクトURL
COGNITO_LOGOUT_URI=http://localhost:3000

# Session Secret
# 32文字以上のランダムな文字列を生成してください
# 生成方法: openssl rand -base64 32
SESSION_SECRET=your-session-secret-at-least-32-characters-long

# Test User Credentials
# テスト用のユーザー認証情報（E2Eテストやブラウザ自動化で使用）
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=your-test-user-password

# Node Environment
NODE_ENV=development
