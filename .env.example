# vim: set ft=sh:

# AWS Cognito Configuration
# Cognito User Pool のドメイン名（例: your-app.auth.ap-northeast-1.amazoncognito.com）
COGNITO_DOMAIN=your-domain.auth.region.amazoncognito.com

# Cognito Application Client ID
COGNITO_CLIENT_ID=your-client-id

# Cognito Application Client Secret（公開クライアントの場合は不要）
# 機密クライアントとして設定した場合のみ必要
# COGNITO_CLIENT_SECRET=your-client-secret

# コールバックURL（開発環境）
COGNITO_REDIRECT_URI=http://localhost:3000/api/auth/callback

# ログアウト後のリダイレクトURL
COGNITO_LOGOUT_URI=http://localhost:3000

# Session Secret
# 32文字以上のランダムな文字列を生成してください
# 生成方法: openssl rand -base64 32
SESSION_SECRET=your-session-secret-at-least-32-characters-long

# Node Environment
NODE_ENV=development
